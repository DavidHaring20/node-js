// We aim to use functional approaches when looping
// like map, filter, reduce, find etc

const failure = ["missclick", "falling down", "skateboard accident", "car accident"
, "tik tok videos", "stepping in mud"];

failure.map(failureObject => {
    console.log(failureObject);
});